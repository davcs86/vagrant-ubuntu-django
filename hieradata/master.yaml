---
consul::config_hash:
  server: true
  datacenter: 'onesim'
  data_dir: '/opt/consul'
  client_addr: '0.0.0.0'
  bind_addr: "%{ipaddress_enp0s8}"
  bootstrap_expect: 1
  log_level: 'INFO'
  ui_dir: '/opt/consul/ui'